id: secrets-scanner
name: Secrets Scanner
layer: 4
version: 1.0
owner: Security Division
priority: critical
status: active
skills: [eng-security-audit]
dependencies: [security-agent]
permissions: [read, block-release]
description: Scans for hardcoded secrets, API keys, tokens, and credentials in code and configuration files.
metrics: { successRate: 95, tasksCompleted: 0, avgCompletionTimeMs: 0, lastActive: null }
