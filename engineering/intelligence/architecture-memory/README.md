# Architecture Memory

Stores approved architectural decisions so Engineering Intelligence does not contradict OpenCode.

```bash
# Sync docs/adr into memory
npm run eng:memory

# Record a new decision
npm run eng:memory -- record --id ADR-41 --title "..." --product DecisionOS --reason "..." --pattern "enforce()"
```

See [INDEX.md](./INDEX.md) after sync.
