# CLAUDE.md

> **DEPRECATED — This file is retained for backward compatibility only.**
>
> Use `docs/AI_ENTRYPOINT.md` as the **single entry point** for every session.
> All operational context, reading order, product status, and agent rules
> are defined there and in `AGENTS.md`.

## Single Entry Protocol

1. Read `docs/AI_ENTRYPOINT.md` first — platform identity, reading order.
2. Follow the reading order defined there (position #0 is AI_ENTRYPOINT.md itself).
3. Follow `AGENTS.md` for agent operating contract and execution rules.
4. Do not rely on this file as authoritative context.

Previous content has been migrated to the documents above.
