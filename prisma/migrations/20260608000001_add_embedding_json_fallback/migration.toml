# AGENT-6: Add embedding_json fallback for pgvector compatibility
# When pgvector extension is not available, embeddings are stored as JSONB in this column.
# The existing `embedding` vector(1536) column is untouched.
provider = "postgresql"