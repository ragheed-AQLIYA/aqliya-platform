# خطة الإصلاح والتطوير الكاملة — منصة عقلية

**التاريخ:** 2026-07-22
**مستند إلى:** تقرير التدقيق الهندسي الشامل AQLIYA_ENTERPRISE_AUDIT_REPORT_2026-07-22.md
**المستوى الحالي:** 78/100 (B)
**المستوى المستهدف:** 92/100 (A) — جاهزية إنتاجية كاملة

---

## المقدمة

هذه الخطة مبنية حصرياً على الواقع البرمجي (Source Code)، وليس على الوثائق أو الادعاءات. كل بند مرتبط بدليل من المستودع. الخطة تغطي جميع الثغرات المكتشفة في التدقيق الهندسي: البنية التحتية، الأمن، الاختبارات، المنتجات، النواة، والهيكلة.

---

## الأولوية القصوى: معالجة معوقات الإنتاج (الأسبوع 1-2)

> **الهدف:** إزالة جميع المعوقات الـ 14 التي تمنع التشغيل الإنتاجي. بدون هذا، لا يمكن النشر.

### المهمة 1.1 — إصلاح حالة Terraform (حرج)

| العنصر | التفاصيل |
|--------|----------|
| **المشكلة** | قاعدة البيانات RDS (`aqliya-prod-db`) غير مسجلة في `terraform.tfstate`. أي أمر `terraform apply` سيحاول إنشاء قاعدة بيانات مكررة. |
| **الدليل** | `infra/terraform/terraform.tfstate` — `rds_endpoint: null` |
| **الإصلاح** | 1. استيراد RDS إلى الحالة: `terraform import aws_db_instance.primary aqliya-prod-db`<br>2. استيراد Redis: `terraform import aws_elasticache_replication_group.main aqliya-prod-redis`<br>3. استيراد S3: `terraform import aws_s3_bucket.uploads aqliya-prod-uploads`<br>4. استيراد ECR: `terraform import aws_ecr_repository.app aqliya/prod/app`<br>5. تشغيل `terraform plan` للتحقق من عدم وجود تغييرات |
| **المرجع** | `docs/deployment/STATE_RECOVERY_RUNBOOK.md` |
| **المدة** | 4 ساعات |

### المهمة 1.2 — إصلاح خطأ اسم البيئة (حرج)

| العنصر | التفاصيل |
|--------|----------|
| **المشكلة** | Terraform يستخدم `var.environment == "production"` لكن ملف `terraform.tfvars` يستخدم `environment = "prod"`. هذا يمنع تفعيل: النسخ الاحتياطي التلقائي، النسخ المتماثل للقراءة، إعادة توجيه HTTP→HTTPS |
| **الدليل** | `infra/terraform/environments/prod/terraform.tfvars` (سطر environment)<br>`infra/terraform/modules/database/main.tf` (`var.environment == "production"`)<br>`infra/terraform/modules/compute/main.tf` (نفس المشكلة) |
| **الإصلاح** | الخيار أ (مفضل): تغيير `"prod"` إلى `"production"` في `terraform.tfvars`<br>الخيار ب: تغيير جميع شروط `"production"` إلى `"prod"` في ملفات Terraform |
| **المدة** | ساعة واحدة |

### المهمة 1.3 — تفعيل اشتراكات التنبيهات SNS (حرج)

| العنصر | التفاصيل |
|--------|----------|
| **المشكلة** | موضوع SNS (`aqliya-prod-alarms`) ليس لديه أي مشترك. التنبيهات (RDS CPU > 80%، مساحة التخزين < 10GB) تُطلق لكن لا أحد يستقبلها. |
| **الدليل** | `infra/terraform/terraform.tfstate` — `aws_sns_topic_subscription.alarm_email: "instances": []` |
| **الإصلاح** | 1. إضافة اشتراك بريد إلكتروني في Terraform<br>2. أو يدوياً: `aws sns subscribe --topic-arn arn:aws:sns:eu-north-1:308621094029:aqliya-prod-alarms --protocol email --notification-endpoint ops@aqliya.com`<br>3. تأكيد الاشتراك من البريد الإلكتروني<br>4. إضافة تنبيهات إضافية: 5xx على ALB، ذاكرة ECS، Redis |
| **المدة** | ساعة واحدة |

### المهمة 1.4 — تغيير كلمة مرور RDS (حرج)

| العنصر | التفاصيل |
|--------|----------|
| **المشكلة** | كلمة مرور RDS في AWS Secrets Manager هي: `"PLACEHOLDER-prod-db-password-rotate-immediately"` |
| **الدليل** | `infra/terraform/terraform.tfstate` — `secret_string: "PLACEHOLDER-prod-db-password-rotate-immediately"` |
| **الإصلاح** | 1. توليد كلمة مرور عشوائية قوية: `openssl rand -base64 32`<br>2. تغييرها في RDS: `aws rds modify-db-instance --db-instance-identifier aqliya-prod-db --master-user-password <new-password>`<br>3. تحديث السر في Secrets Manager<br>4. إعادة تشغيل مهمة ECS لتحميل كلمة المرور الجديدة |
| **المدة** | 30 دقيقة |

### المهمة 1.5 — إزالة ملف terraform.tfstate من المستودع (حرج)

| العنصر | التفاصيل |
|--------|----------|
| **المشكلة** | ملف `infra/terraform/terraform.tfstate` موجود في المستودع ويحتوي على كلمة مرور RDS بنص صريح. هذا تسرب بيانات حساسة. |
| **الدليل** | `infra/terraform/terraform.tfstate` — يحتوي على `secret_string` |
| **الإصلاح** | 1. إزالة الملف من git: `git rm --cached infra/terraform/terraform.tfstate`<br>2. إضافته إلى `.gitignore`<br>3. تدوير جميع الأسرار التي كانت في الملف<br>4. استخدام S3 backend لتخزين الحالة عن بعد<br>5. عمل commit و push force إذا لزم الأمر |
| **المدة** | ساعتان |

### المهمة 1.6 — تفعيل GitHub OIDC للنشر الآلي (عالي)

| العنصر | التفاصيل |
|--------|----------|
| **المشكلة** | `AWS_DEPLOY_ROLE_ARN` غير مضبوط في GitHub Secrets. النشر الآلي من CI/CD معطل. كل النشر يدوي حالياً. |
| **الدليل** | `docs/deployment/OPERATIONAL_CLOSURE_STATUS.md` — البند O3<br>`.github/workflows/deploy.yml` — يحتاج إلى OIDC |
| **الإصلاح** | 1. إنشاء IAM Role مع OpenID Connect provider لـ GitHub Actions<br>2. تعيين الصلاحيات: ECS deploy, ECR push, Secrets Manager read<br>3. إضافة `AWS_DEPLOY_ROLE_ARN` إلى GitHub Secrets<br>4. اختبار النشر الآلي |
| **المدة** | 4 ساعات |

---

## الأولوية الثانية: الإصلاحات الأمنية (الأسبوع 2-3)

### المهمة 2.1 — استبدال حزمة xlsx (حرج أمني)

| العنصر | التفاصيل |
|--------|----------|
| **المشكلة** | حزمة `xlsx` (SheetJS) مهملة منذ 2023. بها ثغرتان: Prototype Pollution (CVSS 7.8) و ReDoS (CVSS 7.5). لا يوجد إصلاح متاح. |
| **الدليل** | `npm audit` — HIGH: GHSA-4r6h-8v6p-xvw6, GHSA-5pgg-2g8v-x4x9 |
| **الإصلاح** | 1. تثبيت `@suryapratap/xlsx` (النسخة المجتمعية المدعومة)<br>2. أو التبديل إلى `exceljs`<br>3. تحديث جميع الاستيرادات في `src/lib/`<br>4. اختبار استيراد/تصدير Excel |
| **الملفات المتأثرة** | البحث عن `from "xlsx"` في `src/` |
| **المدة** | 3 ساعات |

### المهمة 2.2 — ترقية Next.js إلى 16.2.11 (حرج أمني)

| العنصر | التفاصيل |
|--------|----------|
| **المشكلة** | Next.js 16.2.4 به 14 ثغرة أمنية تشمل SSRF (CVSS 8.6)، تجاوز middleware، تجاوز المصادقة. |
| **الدليل** | `npm audit` — HIGH: GHSA-c4j6-fc7j-m34r (SSRF 8.6), GHSA-492v-c6pp-mqqv (middleware bypass 8.1) |
| **الإصلاح** | 1. `npm install next@16.2.11 eslint-config-next@16.2.11`<br>2. تشغيل `npm run build` للتحقق<br>3. تشغيل `npm test` للتحقق من عدم وجود أعطال |
| **المدة** | ساعتان |

### المهمة 2.3 — ترقية nodemailer إلى 9.0.3 (عالي أمني)

| العنصر | التفاصيل |
|--------|----------|
| **المشكلة** | nodemailer 7.0.13 به 6 ثغرات تشمل حقن أوامر SMTP وتجاوز TLS. |
| **الإصلاح** | 1. `npm install nodemailer@9.0.3 @types/nodemailer@9.0.0`<br>2. التحقق من توافق API (تغييرات كبيرة بين 7 و 9)<br>3. اختبار إرسال البريد الإلكتروني |
| **المدة** | 3 ساعات |

### المهمة 2.4 — إضافة CRM webhook إلى middleware (متوسط أمني)

| العنصر | التفاصيل |
|--------|----------|
| **المشكلة** | مسار `/api/crm/webhook` غير مغطى بـ middleware. لا يوجد rate limiting ولا security headers. |
| **الدليل** | `src/middleware.ts` — المطابق لا يشمل `/api/crm/webhook` |
| **الإصلاح** | إضافة `/api/crm/webhook` إلى `publicPrefixes` في `src/middleware.ts` لضمان تطبيق rate limiting و security headers (مع الحفاظ على مصادقة HMAC الخاصة به) |
| **الملفات** | `src/middleware.ts` |
| **المدة** | 15 دقيقة |

### المهمة 2.5 — إصلاح 17 خطأ `as any` (منخفض)

| العنصر | التفاصيل |
|--------|----------|
| **المشكلة** | 17 خطأ `@typescript-eslint/no-explicit-any` في مجلد `src/actions/simulation/` |
| **الدليل** | `npm run lint` — 17 errors in simulation/run/*.ts |
| **الإصلاح** | استبدال `any` بأنواع محددة في 3 ملفات:<br>- `simulation/run/index.ts` (خطأ واحد)<br>- `simulation/run/recommendation.ts` (8 أخطاء)<br>- `simulation/run/simulation.ts` (8 أخطاء) |
| **المدة** | 4 ساعات |

---

## الأولوية الثالثة: تقوية البنية التحتية (الأسبوع 3-4)

### المهمة 3.1 — ترقية RDS من الفئة المجانية (عالي)

| العنصر | التفاصيل |
|--------|----------|
| **المشكلة** | RDS يستخدم `db.t4g.micro` (1GB RAM، 20GB تخزين). غير مناسب للإنتاج. |
| **الإصلاح** | 1. تغيير إلى `db.t4g.medium` (4GB RAM)<br>2. زيادة التخزين إلى 100GB<br>3. تفعيل Multi-AZ للتوفر العالي<br>4. تعديل `terraform.tfvars`: `db_instance_class = "db.t4g.medium"`, `db_allocated_storage = 100` |
| **المدة** | ساعتان (التطبيق يستغرق وقت أطول في AWS) |

### المهمة 3.2 — تفعيل Redis لمحدد المعدل (متوسط)

| العنصر | التفاصيل |
|--------|----------|
| **المشكلة** | محدد المعدل يعمل بالذاكرة فقط. مع وجود مهمتين ECS، يمكن للمهاجم توزيع الطلبات لتجاوز الحدود. |
| **الإصلاح** | تعيين `RATE_LIMITER=redis` في مهمة ECS |
| **الملفات** | ECS task definition environment variables |
| **المدة** | 30 دقيقة |

### المهمة 3.3 — تغيير فحص ALB الصحي (متوسط)

| العنصر | التفاصيل |
|--------|----------|
| **المشكلة** | ALB يستخدم `/api/health` الذي يرجع 200 حتى لو كانت قاعدة البيانات معطلة. |
| **الإصلاح** | تغيير target group health check path إلى `/api/platform/health` (يتحقق من DB + Kernel) |
| **الملفات** | `infra/terraform/modules/compute/main.tf` |
| **المدة** | 15 دقيقة |

### المهمة 3.4 — زيادة عدد مهام ECS إلى 2 (متوسط)

| العنصر | التفاصيل |
|--------|----------|
| **المشكلة** | مهمة ECS واحدة فقط — لا توفر عالي. |
| **الإصلاح** | تغيير `ecs_desired_count` من 1 إلى 2 في `terraform.tfvars` |
| **المدة** | 5 دقائق |

### المهمة 3.5 — جدولة اختبار الاختراق (عالي)

| العنصر | التفاصيل |
|--------|----------|
| **المشكلة** | لم يتم إجراء اختبار اختراق. مطلوب قبل الإطلاق الإنتاجي ولشهادة SOC2. |
| **الإصلاح** | 1. مراجعة `docs/security/PENETRATION_TEST_SCOPE.md`<br>2. التعاقد مع شركة اختبار اختراق خارجية<br>3. جدولة الاختبار<br>4. معالجة النتائج |
| **المدة** | خارجي — 2-4 أسابيع |

---

## الأولوية الرابعة: الإصلاحات الهندسية (الأسبوع 4-6)

### المهمة 4.1 — إصلاح 10 اختبارات فاشلة (عالي)

| العنصر | التفاصيل |
|--------|----------|
| **المشكلة** | 10 اختبارات فاشلة عبر 6 مجموعات. الوثائق تدّعي "zero failures". |
| **الدليل** | `npm test` — 6 failed suites, 10 failed tests |
| **الإصلاح** | 1. تحليل كل اختبار فاشل<br>2. `reporting-graph/graph-builder.test.ts` — edgeType mismatch<br>3. `skill-runtime/skill-evaluator.test.ts` — sample count mismatch (3 vs 2)<br>4. الـ 4 مجموعات الأخرى<br>5. الهدف: 5,710/5,710 نجاح |
| **المدة** | يومان |

### المهمة 4.2 — توحيد قاعدتي SalesOS (حرج للمنتج)

| العنصر | التفاصيل |
|--------|----------|
| **المشكلة** | SalesOS لديه قاعدتي شيفرة متوازيتين:<br>- `src/lib/sales/` (قديم، يعمل، 80+ ملف)<br>- `src/lib/salesos/` (جديد، DDD، مصمم ومختبر لكن لا يوجد مسار يستخدمه!) |
| **الدليل** | البحث عن `from "@/lib/salesos"` في `src/app/sales/` — صفر نتائج |
| **الإصلاح** | **المرحلة أ — إنشاء محولات المسارات:**<br>1. توصيل `createDealAction`, `updateDealAction`, `transitionDealAction` من `@/lib/salesos/api/actions-write` إلى صفحات المسارات<br>2. استبدال قراءة المخزن المؤقت بقراءة `listDealsAction`/`getDealAction` من `@/lib/salesos/api/actions`<br>3. اختبار كل مسار (27 مساراً)<br><br>**المرحلة ب — توصيل حارس الصلاحيات:**<br>4. توصيل guard pipeline (evidence gate, reviewer-not-owner, approval audit) في تدفقات المراجعة/الموافقة<br><br>**المرحلة ج — إضافة تتبع SLA:**<br>5. إضافة مكونات UI لتتبع SLA من `@/lib/salesos/workflow/sla-monitor`<br><br>**المرحلة د — إزالة المخزن المؤقت القديم:**<br>6. إزالة `src/lib/sales/store/` بعد التحقق من عدم وجود مستهلكين<br>7. حذف قاعدة الشيفرة القديمة بعد الترحيل الكامل |
| **الملفات الرئيسية** | `src/lib/salesos/api/actions.ts`, `actions-write.ts`, `src/app/sales/deals/` |
| **المدة** | 3-5 أيام |

### المهمة 4.3 — حذف الاختبار الوهمي (متوسط)

| العنصر | التفاصيل |
|--------|----------|
| **المشكلة** | `src/__tests__/unit/smoke.test.ts` يحتوي على اختبارات وهمية: `expect(1+1).toBe(2)` و `Promise.resolve(42)` — لا تختبر أي كود تطبيقي. |
| **الإصلاح** | 1. حذف الملف<br>2. استبداله باختبار smoke حقيقي يتحقق من تحميل التطبيق<br>أو: إعادة كتابته ليختبر وظائف حقيقية |
| **المدة** | 30 دقيقة |

### المهمة 4.4 — إضافة فحص مصادقة للذاكرة المؤسسية (عالي أمني)

| العنصر | التفاصيل |
|--------|----------|
| **المشكلة** | تخطيط `institutional-memory` هو `"use client"` بدون استدعاء `getCurrentUser()`. أي مسار متداخل يمكن أن يسرب بيانات الذاكرة المؤسسية عبر المؤسسات. |
| **الدليل** | `src/app/institutional-memory/layout.tsx` — لا يوجد `getCurrentUser()` |
| **الإصلاح** | 1. تحويل التخطيط إلى Server Component<br>2. إضافة `const user = await getCurrentUser()`<br>3. إضافة `if (!user) redirect("/login")`<br>4. تمرير user كمصدر للبيانات بدلاً من الاعتماد على العميل |
| **المدة** | ساعتان |

### المهمة 4.5 — إصلاح 3 مسارات تسويقية معطلة (منخفض)

| العنصر | التفاصيل |
|--------|----------|
| **المشكلة** | ثلاثة مسارات تسويقية تعيد 404 لأنها فارغة:<br>- `/how-we-work` — مجلد فارغ<br>- `/engagement-models` — مجلد فارغ<br>- `/buyers/procurement` — مجلد فارغ |
| **الدليل** | الأدلة موجودة بدون `page.tsx` |
| **الإصلاح** | الخيار أ: إنشاء صفحات حقيقية لكل مسار<br>الخيار ب: حذف المجلدات الفارغة<br>الخيار ج: إضافة redirect إلى صفحات بديلة |
| **المدة** | ساعتان |

### المهمة 4.6 — توصيل Outbox Bridge (منخفض، سطر واحد)

| العنصر | التفاصيل |
|--------|----------|
| **المشكلة** | `OutboxBridge` مطبق بالكامل لكن غير موصول بـ EventBus. |
| **الإصلاح** | إضافة سطر واحد في `src/lib/kernel/bootstrap.ts`: `outboxBridge.attach(eventBus)` |
| **المدة** | 5 دقائق |

### المهمة 4.7 — توصيل CQRS ProjectionManager بـ EventBus (منخفض، سطر واحد)

| العنصر | التفاصيل |
|--------|----------|
| **المشكلة** | `ProjectionManager.attachToEventBus()` موجود لكن لم يتم استدعاؤه أبداً. |
| **الإصلاح** | إضافة سطر واحد في `src/lib/kernel/bootstrap.ts`: `projectionManager.attachToEventBus(eventBus)` |
| **المدة** | 5 دقائق |

---

## الأولوية الخامسة: نضج المنصة (الشهر 2-3)

### المهمة 5.1 — إكمال أو إزالة الـ 5 خدمات الوهمية في النواة (متوسط)

| الخدمة | الحالة الحالية | الإجراء |
|--------|---------------|---------|
| `NotificationServiceWrapper` | يولد UUID فقط | تنفيذ حقيقي مع SSE/WebSocket أو إزالته |
| `FilesServiceWrapper` | يرجع NOT_IMPLEMENTED | توصيله بـ S3 |
| `SearchServiceWrapper` | يرجع مصفوفة فارغة | تنفيذ بحث حقيقي أو إزالته |
| `KnowledgeServiceWrapper` | يسجل فقط بدون استرجاع | تنفيذ استرجاع المعرفة أو إزالته |
| `AutomationServiceWrapper` | بدون قواعد مسجلة | تنفيذ محرك أتمتة أو إزالته |

| **المدة** | 5-10 أيام |

### المهمة 5.2 — إضافة AI حقيقي لـ SalesOS (عالي للمنتج)

| العنصر | التفاصيل |
|--------|----------|
| **المشكلة** | SalesOS يستخدم قواعد حتمية فقط (deterministic). لا يوجد LLM. Dashboard يعرض بصدق: "الذكاء التنبؤي الكامل غير مفعّل". |
| **الإصلاح** | 1. توصيل `AIGatewayWrapper` من النواة بـ SalesOS<br>2. إضافة: تحليل ذكي للصفقات، توصيات الخطوات التالية، تحليل المخاطر بالذكاء الاصطناعي<br>3. اتباع قواعد AI Feature (confidence, human review, audit trail)<br>4. اختبار مع `LcAiAuditEvent` كنموذج |
| **المدة** | 5-7 أيام |

### المهمة 5.3 — إثبات تكامل CRM (عالي للمنتج)

| العنصر | التفاصيل |
|--------|----------|
| **المشكلة** | موصلات CRM (Salesforce, HubSpot) موجودة لكن لم يتم إثبات مزامنة خارجية حقيقية. |
| **الإصلاح** | 1. إعداد بيئة اختبار Salesforce/HubSpot<br>2. اختبار `SalesforceConnector` و `HubSpotConnector`<br>3. اختبار محرك المزامنة `sync-engine.ts`<br>4. توثيق عملية التكامل |
| **المدة** | 3-5 أيام |

### المهمة 5.4 — تحسين تغطية ESLint (متوسط)

| العنصر | التفاصيل |
|--------|----------|
| **المشكلة** | `eslint.config.mjs` يحتوي على 200+ سطر من `globalIgnores`. المنتجات الأساسية (AuditOS، SalesOS، LocalContentOS) غير مغطاة بالتدقيق الإملائي. |
| **الإصلاح** | 1. إزالة التجاهلات تدريجياً<br>2. البدء بـ `src/lib/audit/`<br>3. ثم `src/lib/local-content/`<br>4. ثم `src/lib/sales/`<br>5. إصلاح الأخطاء المكشوفة |
| **المدة** | 3-5 أيام (تدريجياً) |

### المهمة 5.5 — تشغيل برنامج جاهزية SOC2 (شهران)

| العنصر | التفاصيل |
|--------|----------|
| **الإصلاح** | 1. إكمال تقييم الفجوة<br>2. معالجة الثغرات المكتشفة<br>3. توثيق السياسات والإجراءات<br>4. التعاقد مع مدقق خارجي<br>5. اجتياز المراجعة |
| **المدة** | 3-6 أشهر |

---

## خارطة الطريق الزمنية

```
الأسبوع 1-2:  ████████████████  معالجة معوقات الإنتاج (6 مهام حرجة)
الأسبوع 2-3:  ████████████████  الإصلاحات الأمنية (5 مهام)
الأسبوع 3-4:  ████████████████  تقوية البنية التحتية (5 مهام)
الأسبوع 4-6:  ████████████████  الإصلاحات الهندسية (7 مهام)
                              ↑ نقطة الجاهزية الإنتاجية (90%)
الشهر 2-3:    ████████████████  نضج المنصة (5 مهام)
                              ↑ الجاهزية المؤسسية (95%)
الشهر 3-6:    ████████████████  شهادات SOC2 / ISO27001
```

---

## مصفوفة الجاهزية بعد الإصلاح

| المنتج/القدرة | الآن | بعد الأولوية 1-4 | بعد الأولوية 5 |
|--------------|------|-----------------|-----------------|
| البنية التحتية | 60% (C) | 85% (B+) | 90% (A-) |
| الأمن | 92% (A) | 95% (A+) | 96% (A+) |
| الاختبارات | 68% (C+) | 85% (B+) | 88% (B+) |
| AuditOS | 92% (A) | 92% (A) | 93% (A) |
| LocalContentOS | 90% (A-) | 90% (A-) | 92% (A) |
| SalesOS | 70% (C+) | 82% (B) | 88% (B+) |
| DecisionOS | 78% (B) | 82% (B) | 85% (B+) |
| النواة | 55% (D+) | 62% (C-) | 78% (B) |
| **المنصة ككل** | **78% (B)** | **85% (B+)** | **92% (A)** |

---

## ملخص التنفيذ

### قبل البدء بأي مهمة تطوير جديدة:
1. ✅ **أوقف** إضافة ميزات جديدة
2. ✅ **أوقف** إنشاء فروع Claude workspace جديدة بدون تنظيف القديمة
3. ✅ **أوقف** الادعاء باكتمال أي شيء بدون تحقق
4. ✅ **أوقف** إضافة استثناءات لـ ESLint

### الأولوية رقم 1 المطلقة:
**استعادة حالة Terraform وإصلاح معوقات البنية التحتية الـ 14.**

بدون هذا، لا يمكن نشر المنصة للإنتاج، بغض النظر عن جودة الكود.

### التكلفة التقديرية للجهد الهندسي:
- **الأولوية 1-4 (4-6 أسابيع):** ~25-35 يوم عمل
- **الأولوية 5 (2-3 أشهر):** ~20-30 يوم عمل إضافي
- **المجموع للوصول إلى 92%:** ~45-65 يوم عمل

### الجدول الزمني للوصول إلى الإنتاج:
- **جاهزية Pilot:** الآن — AuditOS و LocalContentOS جاهزان للتجربة
- **جاهزية إنتاجية كاملة:** 6-8 أسابيع من الآن
- **جاهزية مؤسسية (SOC2):** 3-6 أشهر من الآن

---

*تم إعداد هذه الخطة بناءً على الواقع البرمجي حصراً. كل بند مرتبط بدليل من المستودع. لا توجد افتراضات.*
