# Ontology Extensions

Canonical ontology lives in [`../artifacts/knowledge-ontology.json`](../artifacts/knowledge-ontology.json).

Domain-specific ontology extensions are added per standard during Sessions 1–3.
