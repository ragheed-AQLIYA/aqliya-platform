// Shared helpers for workflow modules.
// Domain-specific helpers are in each module file.
// The shared buildSnapshotData utility is re-exported from ../common.
export { buildSnapshotData } from "../common";
