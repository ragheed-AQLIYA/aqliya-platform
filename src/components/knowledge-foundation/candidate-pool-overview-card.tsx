/**   * Phase 28.3 — Eligible pool visibility for operators.   */  import type { CandidatePoolOverview } from "@/lib/knowledge-foundation/candidate-pool-overview";    export function CandidatePoolOverviewCard({    overview,  }: {    overview: CandidatePoolOverview;  }) {    console.error(JSON.stringify({ event: "KF_RENDER_ENTER", component: "CandidatePoolOverviewCard" }));      const bound = overview.boundTotal;    const unbound = overview.eligibleUnbound;      console.error(JSON.stringify({ event: "KF_RENDER_EXIT", component: "CandidatePoolOverviewCard" }));    return (      <section className="rounded-xl border bg-card p-5 shadow-sm" dir="rtl">        <h3 className="mb-3 text-sm font-semibold">مجمع المرشّحات المُرقّاة</h3>        <div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4">          <PoolStat label="إجمالي المُرقّاة" value={overview.totalPromoted} />          <PoolStat label="متاحة للربط" value={unbound} />          <PoolStat label="مرتبطة بإصدارات" value={bound} />          <PoolStat label="مُدرجة في إطلاق" value={overview.releasedTotal} />        </div>        <p className="mt-3 text-xs text-muted-foreground">          المرشّحات المُرقّاة غير المرتبطة: {unbound} من {overview.totalPromoted}        </p>      </section>    );  }    function PoolStat({ label, value }: { label: string; value: number }) {    return (      <div className="rounded-lg border bg-muted/20 p-3">        <p className="text-xs text-muted-foreground">{label}</p>        <p className="mt-1 text-lg font-bold">{value.toLocaleString("ar-SA")}</p>      </div>    );  } 