// Governance Engine — Context Barrel

export { ExecutionContextBuilder } from './builder';
export type {
  ExecutionContext,
  EntityIndex, EvidenceIndex, ProductIndex, DecisionIndex, AuthorityIndex, SourceIndex,
  ChainMap, RelationshipGraph, GovernanceMetrics,
} from './execution-context';
