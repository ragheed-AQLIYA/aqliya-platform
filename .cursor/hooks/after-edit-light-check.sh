#!/usr/bin/env bash
# Linux/macOS/Cloud Agent equivalent of after-edit-light-check.ps1
inputJson="$(cat)"

echo '{"message":"AQLIYA reminder: after edits, report changed files and suggest targeted validation only."}'
exit 0
